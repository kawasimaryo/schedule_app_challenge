<h1 class="mb-4">スケジュール編集</h1>

<%= render 'form', schedule: @schedule %>

<div class="mt-3">
  <%= link_to '一覧に戻る', schedules_path, class: 'btn btn-secondary' %>
  <%# 詳細ページへ戻るリンクを追加したい場合は以下を有効にしてください %>
  <%#= link_to '詳細へ戻る', schedule_path(@schedule), class: 'btn btn-secondary ms-2' %>
</div>
