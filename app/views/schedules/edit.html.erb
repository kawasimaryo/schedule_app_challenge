<h1 class="mb-4">スケジュール編集</h1>

<div class="card">
  <div class="card-body">
    <%= render 'form', schedule: @schedule %>
    <div class="mt-3">
      <%= link_to '戻る', schedule_path(@schedule), class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
